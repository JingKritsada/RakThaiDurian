export enum OrchardType {
	Sell = "sell",
	Tour = "tour",
	Cafe = "cafe",
	Stay = "stay",
}

export enum DurianStatus {
	Available = "available",
	Low = "low",
	Reserved = "reserved",
	Out = "out",
}

export enum UserRole {
	Owner = "owner",
	User = "user",
}

export enum CropCategory {
	Fruit = "fruit",
	Vegetable = "vegetable",
}
