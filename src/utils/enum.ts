export enum OrchardType {
	SELL = "sell",
	TOUR = "tour",
	CAFE = "cafe",
	STAY = "stay",
}

export enum DurianStatus {
	AVAILABLE = "available",
	LOW = "low",
	RESERVED = "reserved",
	OUT = "out",
}

export enum UserRole {
	ADMIN = "admin",
	OWNER = "owner",
}

export enum CropCategory {
	FRUIT = "fruit",
	VEGETABLE = "vegetable",
	HERBS = "herbs",
}
